O módulo 'location-event' transforma o módulo padrão 'event' do odoo em um módulo para locação de produtos.

'location-event' module trasforms default 'event' module in a module to control product lease
